# 사용법 — Synthetic Client Survey Lab

사회복지기관 실무자를 위한 화면 순서 안내서입니다.
프로그램을 기획할 때 **실제 주민에게 설문하기 전에** 가상 이용자 패널로 먼저 설문을 돌려보고,
어떤 질문을 더 해야 할지 찾는 도구입니다.

> 이 결과는 AI와 합성 페르소나를 이용한 사전 시뮬레이션입니다.
> 실제 이용자·지역주민의 욕구조사를 대체하지 않습니다.

---

## 0. 처음 한 번만 하는 준비

1. 터미널(명령 프롬프트)에서 이 폴더로 이동합니다.
2. 아래 명령을 차례로 실행합니다.

   ```bash
   npm install
   copy .env.example .env           # macOS/Linux: cp .env.example .env
   npm run db:push
   npm run seed                     # 데모 데이터(선택). 둘러보기 좋습니다.
   npm run dev
   ```

3. 브라우저에서 <http://localhost:3000> 을 엽니다.
4. 첫 화면의 **「1분 만에 체험하기」** 버튼을 누르면 데모 데이터(가상 이용자 30명, 설문 6문항,
   실행 결과, 실제 조사 비교 리포트)가 한 번에 만들어집니다.

### 데모 모드란?

API 키를 넣지 않으면 앱이 자동으로 **데모 모드**로 동작합니다.

- 응답을 **Mock(더미) 엔진**이 만들기 때문에 **비용이 들지 않습니다.**
- 같은 조건이면 항상 같은 결과가 나와 화면을 익히기 좋습니다.
- 화면·분석·비교 리포트는 실제 모델을 쓸 때와 **똑같이** 동작합니다.
- 왼쪽 위와 대시보드에 "데모 모드" 표시가 뜹니다.

실제 모델을 쓰려면 `.env` 파일에 API 키를 넣고 서버를 다시 시작하세요(아래 9번 참고).
키를 넣으면 데모 모드 표시가 자동으로 사라집니다.

---

## 1. 프로젝트 만들기

**왼쪽 메뉴 → 프로젝트 → 새 프로젝트 만들기**

| 입력칸 | 예시 |
| --- | --- |
| 프로젝트 이름 | 2027년 노인복지 프로그램 욕구조사 사전실험 |
| 조사 대상 | 65세 이상 지역주민 |
| 설명 | 신규 프로그램 6개 후보 검토 |

만들고 나면 왼쪽 위 **"작업 중인 프로젝트"** 에 자동으로 선택됩니다.
다른 화면으로 이동할 때 이 선택이 계속 따라다닙니다. 화면이 비어 보이면 이 칸을 먼저 확인하세요.

---

## 2. 가상 이용자(Synthetic Clients) 준비하기

**왼쪽 메뉴 → Synthetic Clients**

방법은 두 가지이고, 둘 다 실제 인물이 아닌 가상 데이터를 만듭니다.

### 방법 A. CSV 가져오기 (이미 합성 데이터가 있을 때)

1. **CSV 가져오기** 버튼을 누릅니다.
2. 파일을 선택하거나 내용을 붙여넣고 **컬럼 확인하기** 를 누릅니다.
   (예시 파일: `data/sample/synthetic-clients.csv`)
3. 화면이 컬럼을 자동으로 추천합니다. 필요하면 각 컬럼의 저장 위치를 바꿉니다.
   - **사용자 정의 필드로 보존**: 표준 속성은 아니지만 자료로 남겨둡니다.
   - **저장하지 않음**: 아예 저장하지 않습니다.
4. 이름·전화번호 등 **개인정보 의심 컬럼이 있으면 빨간 경고**가 뜹니다.
   해당 컬럼을 "저장하지 않음"으로 바꾸거나 파일에서 지워야 가져올 수 있습니다.
5. Persona Summary 미리보기로 요약문이 자연스러운지 확인합니다.
6. 데이터셋 이름과 **출처**(예: 기반 데이터, 분포 출처)를 적고 저장합니다.
   출처는 나중에 결과 화면에도 함께 표시됩니다.

### 방법 A-2. NVIDIA Nemotron-Personas-Korea 같은 Parquet 데이터 쓰기

1. 데이터셋 파일(`.parquet`)을 `data/private/` 폴더에 내려받아 둡니다.
2. 터미널에서 컬럼을 먼저 확인합니다.

   ```bash
   npm run personas:csv -- data/private/nemotron-korea.parquet --columns
   ```

3. 필요한 조건으로 CSV 를 만듭니다(아래는 65세 이상 300명, seed 고정).

   ```bash
   npm run personas:csv -- data/private/nemotron-korea.parquet --min-age 65 --limit 300 --seed 42
   ```

4. 만들어진 `data/private/personas.csv` 를 위의 **방법 A** 대로 가져옵니다.

이름·연락처 같은 컬럼은 변환 단계에서 자동으로 제외되고, 나머지 컬럼은 매핑 화면에서
어떤 속성으로 저장할지 고를 수 있습니다.

### 방법 B. 분포로 생성 (통계 비율만 있을 때)

1. **분포로 생성** 버튼을 누릅니다.
2. 인원(예: 200명)과 난수 seed를 입력합니다.
   **같은 seed + 같은 분포 = 항상 같은 패널**이 만들어집니다(재현성).
3. 속성별 비율을 입력합니다. 기본값으로 예시가 채워져 있습니다.

   ```
   연령   65~74세 45% / 75~84세 40% / 85세 이상 15%
   가구형태 독거 38% / 부부 42% / 자녀동거 20%
   디지털  낮음 45% / 보통 35% / 높음 20%
   ```

4. **조건부 규칙(교차표)** 은 "80세 이상이면서 독거면 이동 불편이 이렇게 분포한다" 처럼
   속성 간 관계를 반영합니다. AI가 임의로 상상하지 않고 여기 입력한 값만 사용합니다.
5. **미리보기** 로 생성 예시를 확인한 뒤 **생성하기** 를 누릅니다.

### 확인하기

데이터셋 이름을 클릭하면 패널 구성(연령·가구형태 분포)과 개인별 요약을 볼 수 있습니다.
**요약 보기** 를 누르면 해당 가상 이용자의 Persona Summary가 펼쳐집니다.

---

## 3. 설문지 만들기

**왼쪽 메뉴 → 설문지 → 설문지 만들기**

1. 설문지 이름을 입력합니다.
2. 처음이라면 **데모 설문 6문항 불러오기** 를 눌러 시작하는 것이 빠릅니다.
3. 문항을 추가/수정합니다. 지원 유형:

   | 유형 | 설명 |
   | --- | --- |
   | 단일선택 | 보기 중 1개 |
   | 복수선택 | 최대 선택 개수 지정 가능 |
   | 5점 척도 | 라벨을 바꿔도 내부적으로 1~5로 저장 |
   | 숫자 응답 | 최소·최대·단위 지정 |
   | 주관식 | 자유 문장 |

4. **조건부 표시** 를 쓰면 "Q2 참여의향이 2점 이하일 때만 Q7을 보여줌" 같은 설정이 가능합니다.
   (기준 문항은 앞 순서 문항만 고를 수 있습니다.)
5. ↑ ↓ 버튼으로 순서를 바꾸고 저장합니다.

> **버전 관리**: 이미 실행한 적 있는 설문을 수정하면 자동으로 **새 버전(v2)** 이 만들어집니다.
> 기존 응답은 예전 버전을 그대로 유지하므로, 지난 결과가 바뀌지 않습니다.

---

## 4. 설문 실행하기

**왼쪽 메뉴 → 설문 실행**

1. **1단계**: 설문지와 가상 이용자 데이터셋을 고릅니다.
2. **2단계(선택)**: 대상을 좁힙니다.
   - 인원 상한(예: 100명). 처음에는 5~10명으로 시험해보는 것을 권합니다.
   - 세그먼트 속성을 고르면 **그 데이터셋에 실제로 있는 값이 인원수와 함께** 표시됩니다.
     체크박스로 고르기만 하면 되고, 직접 입력하지 않습니다.
   - 아무것도 고르지 않으면 패널 전체가 대상입니다.
3. **3단계**: AI 설정
   - **Provider**: 비용 없이 시험하려면 `Mock`, 실제 모델은 `Google Gemini` / `Anthropic Claude` / `OpenAI 호환 API`
   - **모델**: 목록에서 고르거나 **직접 입력…** 으로 모델명을 그대로 적을 수 있습니다.
     **사용 가능한 모델 목록 불러오기** 를 누르면 내 API 키로 쓸 수 있는 모델을 실제로 조회합니다.
   - **연결 테스트(1회 호출)**: 실행 전에 키·모델명·JSON 응답 형식이 맞는지 1건만 호출해 확인합니다.
   - **Temperature**: 기본 0.3 (일부 최신 모델은 이 값을 사용하지 않습니다)
   - **반복 실행 횟수**: 2~3회로 하면 응답이 얼마나 일관적인지 확인할 수 있습니다
   - **최대 동시 실행 수**: 기본 5. 호출 한도 오류가 나면 줄이세요
   - **분당 최대 호출 수**: 0이면 제한 없음. **Gemini 무료 키는 12로 자동 설정**됩니다.
     한도 오류가 나면 앱이 스스로 속도를 더 낮추고 기다렸다가 다시 시도합니다
   - System Prompt도 열어서 확인·수정할 수 있고, 실행 기록에 그대로 저장됩니다
4. **4단계**: **예상 호출 수 계산** 을 누르면 이렇게 표시됩니다.

   ```
   대상 응답자: 300명
   설문 실행: 3회
   예상 API 호출: 900회
   ```

   내용을 확인하고 체크박스를 누른 뒤 **설문 실행 시작** 을 누릅니다.

실행 화면에서 진행률이 자동으로 갱신됩니다.
중간에 실패한 응답이 있으면 **실패·미완료 N건 다시 실행** 버튼으로 그 건만 다시 호출합니다.
이미 저장된 응답은 사라지지 않습니다.

---

## 5. 결과 보기

**왼쪽 메뉴 → 결과 분석 → 실행 선택**

- **객관식**: 보기별 응답 수·비율·순위 (막대그래프 + 표)
- **척도**: 평균·중앙값·표준편차·분포
- **주관식**: 자주 등장한 표현과 응답 원문
  (인용문은 모두 "Synthetic Client 응답 예시"이며 실제 주민 발언이 아닙니다)
- **문항 검토 제안**: 한 보기에 70% 이상 몰리거나 아무도 고르지 않은 보기가 있으면 알려줍니다.
  → 실제 설문지에서 보기 구성이나 표현을 고칠 단서가 됩니다.

### 세그먼트 분석

화면 아래 **세그먼트 분석** 에서 문항과 속성(가구형태/연령/이동 불편/디지털 활용 등)을 골라
**비교하기** 를 누르면 집단별 차이를 볼 수 있습니다.

```
병원동행 선호율
독거      41%
부부      24%
자녀동거  18%
```

### 반복 실행 안정성

**실행 기록** 화면 위쪽의 *반복 실행 묶음* → **안정성 비교 보기** 를 누르면
같은 설문을 여러 번 돌린 결과를 나란히 볼 수 있습니다.

```
병원동행   31% / 28% / 33%   변동폭 5%p
스마트폰   22% / 24% / 20%   변동폭 4%p
```

변동폭이 큰 문항은 결과가 불안정하다는 뜻이므로 단독 근거로 쓰지 마세요.

### 내려받기

각 화면의 **요약 CSV / 응답 CSV** 버튼으로 엑셀에서 열 수 있는 파일을 받을 수 있습니다.
(한글이 깨지지 않도록 BOM이 포함되어 있습니다.)

---

## 6. 실제 조사 결과와 비교하기

실제 주민 대상 조사를 마친 뒤 진행합니다.

**왼쪽 메뉴 → 실제 조사 비교 → 실제 조사 결과 업로드**

1. 비교할 설문지를 고릅니다.
2. 익명화된 CSV를 올립니다. (예시: `data/sample/actual-survey.csv`)
   - **이름·전화번호·주소·주민등록번호를 절대 포함하지 마세요.** 자동 검사에서 걸리면 업로드가 막힙니다.
   - 이 데이터는 이 컴퓨터에만 저장되고 **외부 AI로 전송되지 않습니다.**
3. 각 컬럼을 문항에 연결하고 저장합니다.
4. **비교 리포트 만들기** 에서 Synthetic 실행과 실제 조사 데이터를 고르고 리포트를 만듭니다.

리포트에서 볼 수 있는 것:

```
Q1. 가장 참여하고 싶은 프로그램
                 Synthetic   실제    차이
병원동행             31%      26%    +5%p
스마트폰교육         22%      30%    -8%p
건강걷기             19%      18%    +1%p
```

- **차이(%p)**, **순위 차이**
- **MAE(평균 절대 오차)**, **Spearman 순위 상관**
- 척도 문항의 평균 차이
- 주관식에서 두 조사에 공통으로 나온 표현 / 한쪽에만 나온 표현
- 규칙 기반 **자동 해석 문구** (원인을 단정하지 않습니다)

---

## 7. 이럴 때는 이렇게

| 상황 | 해결 |
| --- | --- |
| 화면이 비어 있고 "먼저 프로젝트를 선택하세요"가 뜸 | 왼쪽 위 **작업 중인 프로젝트** 에서 선택 |
| 설문 실행 버튼이 비활성화됨 | 예상 호출 수 계산 → 확인 체크박스 필요 |
| 실행을 눌렀는데 오류가 남 | 예상 대상자가 **0명**이면 실행되지 않습니다. 2단계 세그먼트 값을 다시 고르거나 "사용 안 함"으로 바꾸세요 |
| Provider가 "설정 필요"로 표시됨 | `.env` 에 API 키 입력 후 서버 재시작 |
| 응답이 "형식 오류"로 남음 | 자동으로 2회까지 재시도합니다. 그래도 실패하면 보기 표현을 단순하게 바꾸거나 Mock으로 먼저 점검 |
| 호출 한도 오류가 계속 남음 | 앱이 자동으로 기다렸다 재시도하고 속도도 낮춥니다. 그래도 남으면 잠시 후 **실패·미완료 다시 실행**을 누르세요. 무료 키는 일일 한도가 있어 다음 날 이어서 해야 할 수도 있습니다 |
| 데이터를 처음부터 다시 만들고 싶음 | `npm run db:reset` (**기존 데이터가 모두 삭제됩니다**) |

---

## 8. 결과를 다룰 때 꼭 지킬 것

- 이 결과는 **가설 탐색용**입니다. "실제 주민이 이렇게 답할 것"이라는 예측이 아닙니다.
- 다음 용도로 사용하지 않습니다.
  - 복지서비스 대상자 선정
  - 위험도 판정, 사례관리 개입 우선순위 결정
  - 실제 주민 의견의 대체
  - 의료·정신건강·학대·위기 상황 판단
  - 공식 통계 추정치·대표성 주장
- 보고서에 인용할 때는 반드시 **"AI 합성 페르소나 기반 사전 시뮬레이션 결과"** 임을 함께 적어주세요.
- 권장 표현: 가상 이용자, Synthetic Client, 사전 시뮬레이션, AI 기반 가설 탐색
  피할 표현: 실제 주민 예측, 정확한 욕구 예측, 주민 대표, AI 이용자 조사 결과

---

## 9. API 키 설정 (실제 모델 사용 시)

`.env` 파일을 메모장으로 열어 값을 채운 뒤 서버를 다시 시작합니다.

```bash
# Google Gemini (https://aistudio.google.com/apikey 에서 발급)
GEMINI_API_KEY=AIza...

# 또는
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

DEFAULT_LLM_PROVIDER="gemini"
DEFAULT_LLM_MODEL="gemini-2.5-flash"
```

### Gemini 사용 순서

1. <https://aistudio.google.com/apikey> 에서 API 키를 발급받습니다.
2. `.env` 에 `GEMINI_API_KEY=발급받은키` 를 넣고 저장합니다.
3. 개발 서버를 껐다가 다시 켭니다(`Ctrl+C` 후 `npm run dev`).
4. **설정** 화면에서 Google Gemini 가 "사용 가능"으로 바뀌었는지 확인합니다.
5. **설문 실행** 화면에서 Provider 를 `Google Gemini` 로 고르고
   **사용 가능한 모델 목록 불러오기** → 모델 선택 → **연결 테스트** 를 눌러 성공을 확인합니다.
6. 처음에는 대상 인원 상한을 5명 정도로 두고 실행해 결과를 확인한 뒤 인원을 늘리세요.

> 모델명은 시점·계정에 따라 다릅니다. 반드시 **목록 불러오기**로 확인하세요.
> 신형 모델(`gemini-3.7-flash` 등)은 요청이 몰리면 503(과부하) 오류가 날 수 있습니다.
> 앱이 자동으로 3번까지 다시 시도하고, 그래도 안 되면 해당 응답만 "실패·미완료 다시 실행"으로
> 이어서 처리할 수 있습니다. 급할 때는 한 단계 낮은 모델(`gemini-3.5-flash` 등)을 쓰세요.

- 키는 서버에서만 사용하며 화면이나 데이터베이스에 저장되지 않습니다.
- `.env` 와 데이터베이스 파일은 git에 올라가지 않도록 설정되어 있습니다.
- **설정** 화면에서 현재 사용 가능한 Provider를 확인할 수 있습니다.
- 실제 모델을 쓰면 호출 건수만큼 비용이 발생합니다. 실행 전 예상 호출 수를 꼭 확인하세요.

---

## 10. 전체 흐름 한 장 요약

```
프로그램 아이디어
      ↓
1. 프로젝트 만들기
2. 가상 이용자 패널 준비 (CSV 가져오기 또는 분포로 생성)
3. 설문지 작성 (실제 조사에 쓸 문항 그대로)
4. 설문 실행 (Mock으로 먼저 → 실제 모델로, 반복 2~3회)
5. 결과 분석 (전체 / 세그먼트 / 주관식 / 안정성)
      ↓
   실제 설문지 보완 → 실제 이용자 조사 실시
      ↓
6. 실제 결과 CSV 업로드 → Synthetic과 비교 → 다음 시뮬레이션 개선
```
